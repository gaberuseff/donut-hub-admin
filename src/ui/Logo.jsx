import styled from "styled-components";

const StyledLogo = styled.div`
  font-size: 3rem;
  font-weight: 600;
  text-align: center;
`;

function Logo() {
  return <StyledLogo>Donut Hub</StyledLogo>;
}

export default Logo;
